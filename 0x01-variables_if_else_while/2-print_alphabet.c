#include <stdio.h>
/**
 * main - prints alphabet in lowercase
 *
 * Return: will be 0 for succesfull output 
 */

int main(void)
{
	for (i = 'a'; i <= 'z'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
