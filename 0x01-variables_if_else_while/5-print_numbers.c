#include <stdio.h>

/**
 * main - will print single digit numbers of base ten
 *
 * Return: will be 0 if programme succesful
 *
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
