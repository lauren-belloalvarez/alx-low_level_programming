#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * strtow - splits strings into words
 *
 * @str: input string
 *
 * Return: array of strings if successfull or null if error
 */

char **strtow(char *str)
{
	if (str == NULL || str == "")
	{
		return (NULL);
	}
}
